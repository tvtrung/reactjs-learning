import React from 'react';
import PropTypes from 'prop-types';

Detail.propTypes = {
    
};

function Detail(props) {
    return (
        <div>
            Page Detail
        </div>
    );
}

export default Detail;